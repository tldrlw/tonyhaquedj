{
  "name": "main",
  "version": "1.0.1",
  "description": "GCS → CFv2 → BigQuery ingester",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^8.1.1",
    "@google-cloud/storage": "^7.13.0"
  }
}
